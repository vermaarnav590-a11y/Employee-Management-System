# Employee-Management-System
A Python Employee Management System using SQLite for persistent data storage. It supports Create, Read, Update, Delete (CRUD) operations through a simple command-line interface.
